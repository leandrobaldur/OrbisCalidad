import React, { useState } from 'react';
import axios from 'axios';

const RegistroEmpresa = ({ ancho = '100%', alto = '100%' }) => {
  
  return (
    <RegistroEmpresa ancho="80vw" alto="85vh" />

  );
};

export default RegistroEmpresa;
